const visitReason = {
  labels: [ 'Covid Test', 'Injury', 'Illness', 'Others' ],
  series: [ 30, 15, 20, 5 ]
}

const visitStatus = {
  labels: [ 'Discharged', 'Cancelled', 'No-Show' ],
  series: [ 40, 20, 10 ]
}

export { visitReason, visitStatus }
